name: Echo tag message

on:
  push:
    tags:
      - 'tm-*'

jobs:
  doTheThing:
    runs-on: ubuntu-latest

    steps:
      - name: get tag message
        run: git show -s --pretty='' ${{ github.ref_name }}
      # - name: echo webhook
      #   run: echo "commit message ${{ github.event.description }}. commits ${{ toJSON(github.event) }}"
