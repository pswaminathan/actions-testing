name: Echo tag message

on:
  push:
    tags:
      - 'tm-*'

jobs:
  doTheThing:
    runs-on: ubuntu-latest

    steps:
      - name: echo webhook
        run: echo "webhook ${{ github.commits[0].message }}. commit message ${{ github.head_commit.message }}"
